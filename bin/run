#!/usr/bin/env node
const exec = require("../lib");

if (process.argv.length < 2) {
  console.log("usage: artifact-notify [path-to-artifact]");
}

const [targetPath] = process.argv.slice(2);

exec(targetPath);
